# vcpu

A small virtual CPU emulator for Windows. It has its own 64-bit
instruction set, an assembler, and an interpreter that runs over a flat
VirtualAlloc-backed address space.

Early days, still building it out.
